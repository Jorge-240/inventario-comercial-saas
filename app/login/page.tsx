'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export default function LoginPage() {
  const router = useRouter(); const [email, setEmail] = useState('admin@demo.com'); const [password, setPassword] = useState('admin123'); const [error, setError] = useState(''); const [loading, setLoading] = useState(false);
  async function submit(e: FormEvent) { e.preventDefault(); setLoading(true); setError(''); const res = await fetch('/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password }) }); const data = await res.json(); if (!res.ok) setError(data.error); else router.push('/dashboard'); setLoading(false); }
  return <main className="min-h-screen bg-gradient-to-br from-brand-700 via-brand-500 to-cyan-400 flex items-center justify-center p-6"><div className="w-full max-w-5xl grid md:grid-cols-2 overflow-hidden rounded-3xl bg-white shadow-2xl"><section className="hidden md:flex flex-col justify-between bg-slate-950 p-12 text-white"><div><div className="mb-10 flex items-center gap-3 text-xl font-bold"><span className="rounded-xl bg-brand-500 p-2">▦</span> Stockly</div><h1 className="text-4xl font-bold leading-tight">Todo tu inventario bajo control.</h1><p className="mt-5 text-slate-300">Una plataforma simple, rápida y segura para hacer crecer tu negocio.</p></div><p className="text-sm text-slate-400">Gestión comercial inteligente · Multiempresa</p></section><section className="p-8 md:p-12"><div className="mb-10 md:hidden flex items-center gap-3 text-xl font-bold"><span className="rounded-xl bg-brand-500 p-2 text-white">▦</span> Stockly</div><h2 className="text-3xl font-bold">Bienvenido de nuevo</h2><p className="mt-2 text-slate-500">Ingresa a tu espacio de trabajo</p><form onSubmit={submit} className="mt-8 space-y-5"><label className="block text-sm font-semibold">Correo<input className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-brand-500" type="email" value={email} onChange={e => setEmail(e.target.value)} required /></label><label className="block text-sm font-semibold">Contraseña<input className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-brand-500" type="password" value={password} onChange={e => setPassword(e.target.value)} required /></label>{error && <p className="rounded-lg bg-red-50 p-3 text-sm text-red-600">{error}</p>}<button disabled={loading} className="w-full rounded-xl bg-brand-600 py-3 font-bold text-white transition hover:bg-brand-700 disabled:opacity-60">{loading ? 'Ingresando...' : 'Iniciar sesión'}</button></form><p className="mt-6 text-center text-xs text-slate-400">Demo: admin@demo.com / admin123</p></section></div></main>;
}
